const string = 'Hello';

//Rellena el valor completando los caracteres faltantes
//con los caracteres colocados al inicio
console.log(string.padStart(7, 'hi')); 

console.log(string.padEnd(6, '-')); //Realiza lo mismo al  caso anterior pero en el final
